#include<cmath>
#include<iostream>


using namespace std;


int main()
{
	int x,n;
	cout<<"Enter a number: ";
	cin>>x;
	cout<<"Enter it's power: ";
	cin>>n;
	int result=pow(x,n);
	cout<<n<<"th power of "<<x<<" is "<<result<<endl;
	x=60;p
	int y=cos(x);
	cout<<"Cosine of "<<x<<" is "<<y<<endl;
	
	
	
	return 0;
}
